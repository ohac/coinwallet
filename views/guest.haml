!!!
%html
  %head
    = partial :htmlheader, :locals => { :nickname => nil }
  %body
    %a{:href => '/auth/twitter'} Login with Twitter
    %br
    %a{:href => '/auth/github'} Login with GitHub
    %br
    %p The total number of accounts: #{accounts.size}
    - balances.each do |coinid, balance|
      - sym = coins[coinid.to_s]['symbol']
      - price = prices[sym.to_sym] || { :bid => 'n/a', :ask => 'n/a' }
      #{coinid}: #{"%.4f" % balance} #{sym}
      ( Bid #{price[:bid]} XRP, Ask #{price[:ask]} XRP )
      %br
